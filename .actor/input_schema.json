{
  "title": "TotalJobs Scraper",
  "description": "Scrape jobs from Totaljobs.com. Provide either keyword/location/category OR a Totaljobs search URL (startUrl, url, or startUrls). Actor paginates and optionally scrapes detail pages.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Start scraping from a specific Totaljobs search URL. If provided, this will be used instead of Keyword/Location.",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Keyword (if not using Start URL)",
      "type": "string",
      "description": "Job search keyword(s), e.g., 'Software Developer', 'Marketing Manager', 'Data Analyst'.",
      "editor": "textfield",
      "prefill": "software engineer"
    },
    "location": {
      "title": "Location (if not using Start URL)",
      "type": "string",
      "description": "Location to filter results (leave empty for broad results).",
      "editor": "textfield"
    },
    "postedWithin": {
      "title": "Posted within (days)",
      "type": "string",
      "description": "Filter jobs posted within the last N days. Leave empty for all jobs.",
      "editor": "select",
      "enum": ["1", "3", "7"],
      "enumTitles": ["Last 24 hours", "Last 3 days", "Last 7 days"]
    },
        "collectDetails": {
      "title": "Collect job details",
      "type": "boolean",
      "description": "If enabled, scraper opens job detail pages for full description.",
      "editor": "checkbox",
      "default": true
    },
    "results_wanted": {
      "title": "Maximum number of jobs",
      "type": "integer",
      "description": "The maximum number of jobs to collect. Leave empty to collect all available jobs.",
      "minimum": 1,
      "default": 100,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum number of pages",
      "type": "integer",
      "description": "A safety cap on the number of search result pages to visit.",
      "minimum": 1,
      "editor": "number",
      "default": 20
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Use Apify Proxy (datacenter or residential depending on your infrastructure).",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      }
    },
    "cookies": {
      "title": "Custom cookies (raw header)",
      "type": "string",
      "description": "Optional raw 'Cookie' header string. Can help bypass consent banners.",
      "editor": "textfield"
    },
    "cookiesJson": {
      "title": "Custom cookies (JSON)",
      "type": "string",
      "description": "Optional JSON-formatted cookies. Accepts an array (e.g., [{\"name\":\"a\",\"value\":\"1\"}]) or an object map (e.g., {\"a\":\"1\"}).",
      "editor": "textarea"
    },
    "dedupe": {
      "title": "Deduplicate results",
      "type": "boolean",
      "description": "Remove duplicate job URLs from results (in-memory deduplication).",
      "editor": "checkbox",
      "default": true
    },
    "maxConcurrency": {
      "title": "Max concurrency",
      "type": "integer",
      "description": "Upper bound on parallel requests. Leave empty to use the fast default (12).",
      "editor": "number",
      "minimum": 1
    },
    "minConcurrency": {
      "title": "Min concurrency",
      "type": "integer",
      "description": "Lower bound on parallel requests (defaults to half of max). Leave empty for automatic tuning.",
      "editor": "number",
      "minimum": 1
    },
    "maxRequestsPerMinute": {
      "title": "Max requests per minute",
      "type": "integer",
      "description": "Throttle outbound requests. Leave empty to use fast default (220 RPM).",
      "editor": "number",
      "minimum": 30
    },
    "navigationDelayRange": {
      "title": "Navigation delay range (ms)",
      "type": "array",
      "description": "Two numbers [min, max] for the random delay before navigation (defaults to [30,120]).",
      "editor": "array",
      "items": {
        "type": "integer",
        "minimum": 0
      },
      "minItems": 2,
      "maxItems": 2
    },
    "listingDelayRange": {
      "title": "Listing delay range (ms)",
      "type": "array",
      "description": "Two numbers [min, max] for waits between listing page operations (defaults to [120,280]).",
      "editor": "array",
      "items": {
        "type": "integer",
        "minimum": 0
      },
      "minItems": 2,
      "maxItems": 2
    },
    "detailDelayRange": {
      "title": "Detail delay range (ms)",
      "type": "array",
      "description": "Two numbers [min, max] for waits on detail pages (defaults to [220,520]).",
      "editor": "array",
      "items": {
        "type": "integer",
        "minimum": 0
      },
      "minItems": 2,
      "maxItems": 2
    },
    "blockDelayRange": {
      "title": "Block delay range (ms)",
      "type": "array",
      "description": "Two numbers [min, max] for cooldowns after blocks/transport errors (defaults to [1200,2000]).",
      "editor": "array",
      "items": {
        "type": "integer",
        "minimum": 0
      },
      "minItems": 2,
      "maxItems": 2
    }
  },
  "required": [],
  "additionalProperties": false
}
